/**
 * Negative prompt por defecto, para las herramientas sueltas que no cargan
 * el guión completo. El de los reels vive en src/cinematic/characters.ts.
 */
export const NEGATIVE_FALLBACK =
  'text, watermark, logo, brand names, distorted face, extra fingers, deformed hands, ' +
  'plastic skin, oversaturated, cartoon, 3d render, blurry, low quality'
